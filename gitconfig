[push]
  default = tracking
[format]
  pretty = %Cblue%h%Creset %Cgreen[%ar]%Creset (%an) %s
[alias]
  st = status
  ci = commit -v
  up = !git fetch origin && git rebase origin/master
  mm = !git checkout master && git merge @{-1} --ff-only
[user]
  name = Dan Collis-Puro
  email = djcp@thoughtbot.com
